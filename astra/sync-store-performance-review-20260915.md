# SyncStore 逐行审核与性能整理

基线为 `aadcbb4`, 包含维护者新增的二分定位和预分配提案. 本轮审核范围是
`sync_store.hpp/.cpp`、两个存储测试文件及新增存储微基准, 不表示重新逐行审核整个仓库.
修改保留在工作区, 未提交或推送. 结构化结果见
[测试与微基准数据](../testkit/results/store-performance-review-20260915.json).

## 最终实现

- 用 `std::ranges::upper_bound` 的成员投影定位版本, 省去比较 lambda.
- 用 `std::ranges::subrange` 表达后缀, 先统计记录数并检查容量, 再一次预留结果数组.
  保留实测表现更稳妥的迭代器 `insert`, 不强行使用 `append_range`.
- 将 `put` 中可以提前完成的 Key 复制和记录数组分配移到锁外, 锁内只补上提交序号.
- `emplace_back` 直接构造聚合记录, 避免为了追加记录再显式创建临时对象.
- 去掉 `StoreEntry::deleted`, 由空 `payload` 表达可复用的删除节点.
  空载荷指针与合法的零字节载荷不同, 对外 `DeltaRecord::deleted` 继续保留.
- 保留节点版本, 用于防止旧删除记录淘汰时提前回收新近删除的可复用节点.
  版本不是新增业务代次, 不承担 Catalog CAS 或跨 Star 身份判断.
- 修复 `evict_expired(max())` 误删永不过期数据的问题.
- 明确 `current_version` 是完整结果的确认屏障, 完整应用之前不能提前推进游标.

## 逐函数审核

| 位置 | 审核结论与处理 |
| --- | --- |
| `DeltaRecord` / `SyncResult` / `Snapshot` | Key 和返回容器独立拥有; 只共享不可变 payload. 补齐每个字段的语义和生命周期注释. |
| 构造函数 | 容量限制完整批次数, 0 容量有效, 不是字节预算. 未引入动态配置或新计数状态. |
| `put` | 在锁外准备可提前执行的分配. 锁内新节点仍有失败回滚; 历史追加成功后才发布值与版本. |
| `remove` | 不存在或空节点时不推进版本; 删除指令先分配后提交. 复用节点避免反复分配长 Key 与 Map 节点. |
| `evict_expired` | 先收集完整批次, 再提交. 有限截止才能到期, max 哨兵始终跳过. 未增加堆索引或额外时钟状态. |
| `global_version` | 与 Map/历史共用一把锁, 不引入可能与当前状态不同步的 atomic 计数器. |
| `get_snapshot` | 同版本复用同一快照, 新快照完整构建后才替换缓存. 分配失败不破坏已发布快照. |
| `create_snapshot_locked` | 一次扫描有效值, Map 节点和 Key 仍需分配. 未把 shared_ptr 误称为整个快照零拷贝. |
| `extract_since` | 严格排除游标本身, 整批返回. 历史不足、未来游标及当前版本快路径保留. 记录求和防溢出. |
| `next_version_locked` | 耗尽显式失败, 不允许回绕破坏历史排序. 此极限分支仅作静态检查, 未声称已动态覆盖. |
| `trim_history_locked` | 只淘汰完整批次; 有效重建值不能删除, 新近删除的节点继续按版本保留复用机会. |
| 两个测试文件 | 增补极值、长历史、结果生命周期、重复删除/重建及查询分配失败测试; Release 断言仍有效. |

成员变量、局部变量、函数参数/返回值、异常与所有权约束及关键代码块均补充中文说明,
使用英文标点. 函数签名和左花括号保持同一行, 继续使用现有 `BreakBeforeBraces: Attach`.
未将非空函数体压成一行, 未全局重排其他模块.

## 实测后撤回的方案

立即从 Map 删除节点能减少残留 Key 和快照扫描, 但删除后重建同 Key 的实测耗时增加约
25%–50%, 因而撤回. 仅保留空节点却删除版本字段, 会让较早的删除记录过早回收新近的
空节点, 同样削弱节点复用, 因而保留节点版本.

按有效条目精确统计快照桶容量需要多扫描一遍 Map, 本轮实测没有证明收益, 因而继续使用
节点数量作为容量上界. 代价是空节点多时可能预留多余桶, 此项取舍被明确保留.

`append_range` 与 `insert` 也单独比较过. 新接口没有展示一致优势, 短 Key 的一些批次场景
反而更慢, 因而最终使用 `insert`. 高级语言设施以减少代码和保持行为清楚为目的,
本轮没有引入自定义内存池、无锁结构、额外版本状态或精确存活计数器.

## 性能结果的边界

GCC 16.2.0, C++26, `-O3 -DNDEBUG`, 开启项目原有 STL 断言和契约配置.
三份实现使用同一微基准源码: 原提交、维护者最初的二分/预分配提案、最终实现.
每份运行两次, 正反次序交替; 每次每个场景执行七轮并取 ns/op 中位数.
计时包含结果销毁, 不包含数据准备和进程启动. 完整 16 个场景保留在 JSON 中, 未只保存最好结果.

代表性结果如下, 区间是两次中位数的最小/最大值, 不是置信区间:

| 场景 | 原提交 | 最终实现 |
| --- | ---: | ---: |
| 1000 批历史, 8 字节 Key, 补 1 批 | 1414–1426 ns | 72–88 ns |
| 1000 批历史, 8 字节 Key, 补 16 批 | 1624–1827 ns | 229–263 ns |
| 1000 批历史, 8 字节 Key, 全部重放 | 33.7–37.9 us | 20.2–22.4 us |
| 1000 批历史, 128 字节 Key, 全部重放 | 138.1–140.9 us | 133.0–153.3 us |
| 删除后重建同 Key | 159–160 ns | 154–185 ns |
| 全部存活的 1000 Key 快照 | 129.0–134.3 us | 137.9–158.9 us |

最确定的收益来自避免扫描已确认历史, 主要由维护者的原始思路带来. 完整重放长 Key
受字符串分配和复制影响, 没有稳定提升; 快照和写入的测量也有波动, 部分轮次更慢.
本轮不宣称所有接口都加速, 也不把虚拟机单线程微基准当作争用吞吐、尾延迟或生产容量结论.
锁外准备减少的是临界区内的工作, 尚未单独测量多线程争用收益.

## 验证结果

- Debug、Release、ASan/UBSan、TSan: 两个存储测试程序均通过, 共 8 个配置/程序组合.
- 每个故障测试实际触发 76 次写入分配失败、7 次查询分配失败; 所有失败必须显式传播.
- 新的 max 哨兵测试在原提交上失败, 最终实现通过, 已保留失败日志作为回归证据.
- 新增长历史测试覆盖 1128 次提交、1000 批保留窗口及不同位置的精确后缀.
- 覆盖旧快照/增量在 Store 销毁后仍有效, 空 Key/空载荷不是删除, 旧删除记录淘汰不影响重建.
- clang-format 和 diff 空白检查通过, 新增 CMake 目标 `star_sync_store_bench` 构建通过.
- 未采集行/分支/函数覆盖率, 不将通过率当作覆盖率. 本轮没有重跑网络服务或 SDK 回归.

对过期哨兵错误检索了其他 SDK、旧 Cluster 和 Supervisor 实现, 未发现对应的 `SyncStore`
或相同租约哨兵清理逻辑. 这不是其他语言全量代码审计的替代结论.

原始日志、各候选方案结果及最终源码哈希保存在 `build/store-review-20260915/`.
未下载依赖或安装工具, 未启动长期测试. 上一轮整体骨架报告继续对应旧提交,
不自动扩展为本轮修改的完整服务回归证明.

## 复现当前微基准

使用已经准备好的 Linux 项目工具链和依赖, 在仓库根目录执行:

```bash
bash cluster-cpp/build.sh configure --profile release --benchmarks
cmake --build build/cluster-cpp/release --target star_sync_store_bench --parallel 1
build/cluster-cpp/release/star_sync_store_bench
```

比较旧版本时应将相同 `bench/sync_store.cpp` 分别链接到对应版本的 `sync_store.cpp/.hpp`,
保持编译参数一致. 不把不同测试输入、未消费的结果或重建开销混入对比.
