# C++ 编码规范 (极简与高性能)

这是本代码库 (Verdandi/Astra) 的核心编码铁律。Agent 在编写、重构或审查 C++ 代码时，必须严格遵守以下原则：

1. **极简的单名词命名 (Single-Word Naming)**
   - 偏好极致的单单词命名。
   - 能够用一个词（例如 `sweep`, `stale`, `capacity`, `version`）表达的，绝不带前缀，绝不拼凑动宾短语或长尾修饰词。
   - 去除类似 `get_` 这种无意义的前缀，C++ 的 Getter 函数直接使用单名词。

2. **极小作用域内敛 (Minimal Scope)**
   - 结构体、枚举和内部类型必须嵌套入其强相关的归属类 (class/struct) 内部。
   - 绝不允许在顶级命名空间 (如 `astra::`) 中暴露松散的外围结构体。

3. **强类型包装 (Strong Typing)**
   - 避免直接传递裸奔的基础类型。
   - 倾向于将关联的数据组合打包为专用的 `struct` 强类型，并在调用栈中传递以保证类型安全。

4. **性能上“重值轻键” (Value Over Key)**
   - 对于大型且不可变的业务负载（如二进制 Payload），必须通过共享引用 (`std::shared_ptr<const std::vector<std::uint8_t>>` 等并配合 `using` 别名）流转，严禁大对象内存拷贝。
   - 对于系统内的 Key（如路由、标识），坚决使用 `std::string` 值传递，充分利用 C++ 的短字符串优化 (SSO) 以及提升哈希表的 Cache 命中率。避免不必要的 `shared_ptr` 封套。

5. **绝不硬编码魔法数字 (No Magic Numbers)**
   - 底层的界限约束（如数组长度、包体积大小上限、超时阈值）必须具有严密的物理或网络依据。
   - 必须将其抽取为具名常量（`constexpr`）或通过配置结构体 (`Config`) 进行外部透传，严禁裸露的数字硬编码。
