# 项目工作约定

## C++ 编码规范

- 后续会话涉及本仓库 C++ 的设计、实现、整理或审核时, 必须先阅读并遵循独立的 [C++ 编码规范](cpp-coding.md).
- 这是用户已确认的项目习惯, 不需要用户在新会话中重复说明. C++ 的命名、作用域、性能与精简取舍、注释和排版以该文为准, 其他通用质量及授权约定继续适用.
- 规范覆盖手写生产代码、测试代码及模板. 不得为落实该规范修改生成代码或第三方代码.

## 测试需要明确授权

- 普通构建、回归或性能测试授权默认不包含 Sanitizer 及其前置构建. 只有用户在本轮明确要求“完整测试”或点名 ASan、UBSan、TSan 等检查时才启动, 不在每轮回归中自动附加.

- 修改、整理、优化或审核代码后, 先汇报改动及建议测试范围, 等待用户明确同意再运行测试.
- 未取得本轮授权, 不自动启动测试及其前置构建, 包括单元测试、回归、Sanitizer、性能基准、压力及长期测试; Windows 和 Linux 均适用.
- 可以先编写测试用例、静态审阅代码和执行规定的源码格式化, 但不得执行测试或通过其他脚本隐式触发测试.
- 用户当次明确要求运行测试, 或批准具体测试范围, 即视为授权; 在该范围内不重复询问. 历史轮次的测试授权不延续为以后每次修改的自动授权.
- 用户要求停止时, 停止本任务持有的测试并清理其资源, 不影响其他任务或部署服务.

软件、工具与第三方依赖的下载和安装仍遵循用户的全局约定, 测试授权不等于下载授权.

## 方案选择与等待

- 需要用户选择的事项分批使用普通聊天文字编号提出, 不调用弹出选择界面的提问工具, 不使用倒计时式选择或自动采用预选项.
- 一批问题提出后暂停后续修改与实现, 等待用户明确答完本批再继续. 用户只回答部分时, 记录已答项并只询问剩余项, 不自行补全.
- 未答复、等待时间经过或推荐标记都不构成用户选择或授权; 不以默认方案继续执行.

## Linux 测试并行策略

- 用户已将 Ubuntu 测试虚拟机配置为 16 核心, 内存上限 8 GiB. 后续获得测试授权后, 优先并行构建和执行相互独立的测试, 缩短反馈时间.
- 内存上限不等于当前已分配或可用内存. 每轮先检查实际 CPU, 可用内存及资源限制, 按编译和测试的内存开销选择并行度; 不继续固定单任务, 也不直接按 16 核开启 16 个高内存任务.
- 编译并行度与测试并行度分别控制, 所有并发配置共享同一资源预算. Sanitizer 等高内存任务适当降低并行度, 避免多配置同时构建或运行导致 OOM.
- 只有端口, 临时目录, 数据和进程资源相互隔离的用例才可并行. 尊重 CTest 的 RUN_SERIAL, RESOURCE_LOCK 等约束; 存在共享资源或时序依赖的场景仍串行执行.
- 此偏好不构成新的测试或下载授权, 也不要求为了利用全部核心牺牲结果可靠性.

## 系统测试拓扑

- 标准系统集成和性能模型从 3 台 Star 起步, 每台同时接受自己的本地写入, 并覆盖跨节点订阅和双向来源复制. 不能只启动多台却只向首台写入.
- 单 Star、双 Star 和内存组件用例保留为定向诊断及边界验证, 不替代标准多节点验收. 对照 Redis 时保持总资源和应用负载口径明确, 将复制、SDK 及控制面开销纳入报告.
- 测试模型要求不构成本轮运行授权, 仍先完成修改和静态检查, 等待用户明确批准测试.

## 文档维护

- 当前文档入口为 [docs/README.md](docs/README.md). 只维护最新有效规范、设计与验证, 不创建工作区历史归档或新的日期化报告.
- 历史证据使用 Git 查询; 最新测试记录维护在 testkit/validation.md, 保留实际日期、源码身份与验证边界, 不把未运行的改动记为通过.
- codex.md 和 GEMINI.md 仅链接统一约定, 不另行复制规则或累计会话日志.

## Git 与发布

- `main` 是公开发布分支. 当前 Markdown 发布白名单仅包含根目录 `README.md`, 其他内部文档保留在 `alpha`; 修改此规则需要用户明确决定. `LICENSE` 继续作为公开的非 Markdown 许可证文件.
- 发布从 `main` 选择明确获准的文件或提交, 不通过普通合并将 `alpha` 的内部文档整体带入. 公开协议文档也须先明确发布方式或调整白名单.
- 只有用户在当前任务中明确要求才创建、修改或签署提交; 推送分支或标签需要明确指令. 推送 `main` 不授权推送 `alpha`, 以前的授权不延续到本次改动.
- 新工作副本使用 `alpha` 前须恢复本地推送保护. 获准推送只放行本次审阅的确切提交, 不关闭保护或形成后续推送的长期授权.
- 工作前后检查 Git 状态, 保留用户现有改动; 不擅自丢弃、重置或覆盖.
