// 场景通道约定; 默认普通层可单次绘制, 透镜启用时将光学体积和辅助标记隔离.
export const renderLayers = {
  // 实体、天空和远景星点, 捕获为可被引力透镜采样的背景.
  scene: 0,
  // 黑洞光学体积, 共享背景而不递归折射其它黑洞.
  optics: 1,
  // 轨道线和选中环, 在光学合成之后绘制.
  overlay: 2,
} as const;
