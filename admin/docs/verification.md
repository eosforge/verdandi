# 验证说明

## 自动检查

运行 `pnpm check`, 顺序执行格式、依赖边界、Node 回归测试、严格类型检查和 Vite 生产构建.
这些检查使用已存在的依赖, 不会安装测试框架、浏览器或其它工具.

| 范围 | 自动覆盖                                                                              |
| ---- | ------------------------------------------------------------------------------------- |
| 数据 | 演示规模、确定的球面布局、重复 ID、错误归属、非法坐标、悬空和重复边                   |
| 相机 | 推进起止和重定向、取消、滚轮连续性、帧率独立性、锚点保持、距离上下限                  |
| 调度 | 30/32/60/120/144 Hz 时钟、真实 FPS、重复启动、后台恢复、长帧、绘制异常                |
| 输入 | 单击资格、中键不拾取、拖动回到起点不误选、多指/取消、双击、Escape、滚轮单位、监听释放 |
| 对象 | 九批 144 实例、矩阵上传、5/2/停止、分层拾取、恒星大小节点、缩放拾取、自转独立与停转   |
| 轨道 | 焦点椭圆、周期、近快远慢、全周期安全间距、跨星系障碍、重合种子、确定性与快照不变      |
| 资产 | 本地 GLB 解码、共享资源释放、无外部引用、加载错误、加载前及加载中取消                 |
| 清理 | 逆序、幂等、单项失败后继续清理、晚到资源立即释放                                      |

对象测试只替代岩质纹理生成所需的二维像素容器, GLB 解码、数学和射线拾取使用真实 Three.js.
这些用例不验证 GPU shader、真实驱动、浏览器默认手势或 Vue 模板运行时.

## 浏览器验收清单

修改界面或运行时后, 在已有浏览器中访问开发服务; 发布前还应使用 `pnpm preview` 检查构建产物.

1. 首屏显示侧栏、三颗可用恒星及行星、独立 Orion 黑洞和右上角 FPS; 只有可用节点之间有连线, 控制台无运行错误.
2. 折叠和展开侧栏, 画布填满剩余区域, 星图比例正常.
3. 点击恒星连续推进, 不自动出现详情; 左键旋转和中键平移可中断推进.
4. 滚轮向内/向外缩放连续, 在距离上下限附近无跳变或 NaN.
5. 总览无法点击行星; 进入星系后仅能选择所属行星, 详情显示正确的 ID、类型、Peer 和演示来源; 暂停所有行星公转.
6. 切换实体类型、选择列表行, 仅目标行高亮; “推进至行星”仍指向选中的实际位置.
7. 双击画布、画布聚焦时按 Escape、详情关闭按钮都返回全景并清空详情.
8. 隐藏页面后停止绘制, 恢复后无动画追赶; 热更新或卸载后画布、监听和 RAF 不重复.
9. 在允许的开发测试环境模拟无 WebGL 或上下文丢失, 显示错误; 重试只创建一个新画布.
10. 替换数据快照后旧场景释放, 新快照从总览展示; 空快照不崩溃, 非法快照显示错误.

第 8-10 项涉及浏览器生命周期, 未执行的项目必须明确记录, 不能用纯函数测试代替.

## 黑洞固定视角对照

开发服务中的 `/tests/visual/black-hole.html` 直接使用生产 GLB、材质和实例工厂, 固定 DPR 为 1,
提供 0、-2、+2、12、45、90 度和距离 18、48、96、192 的按钮. 默认暂停流纹, 可单独播放,
便于对照盘缘连接、宽次像和缩放过滤. 页面不作为生产构建入口, 不额外安装工具.
这里展示原始模型局部单位; 完整星图会把整个黑洞缩放为 0.75.

`/tests/visual/galaxy-controls.html` 提供生产控制器的按钮和结果, 便于复核总览、跨星系和有效行星选择.
也可显式设置 Atlas 自转为 0.3 rad/s 或停转; 该速度仅为验收输入, 不作为生产场景默认值.
它不读写场景内部状态, 也不绕过选择权限; 页面关闭时释放场景, 不作为生产构建入口.

## 2026-09-10 生成时检查行星运动安全

- 按维护者补充要求只在生成时检查, 不使用逐帧速度让行、排斥或离轨避让.
  轨道允许不同时间经过同一区域; 通过整数分频的共同周期使整组相位可重复, 保留 Kepler 周期关系.
- 全周期检查使用模型包围半径、安全余量和曲线插值误差, 比较同步扫掠线段, 包含不同星系、类型及恒星核心.
  规划会改变布局参考中的实际起点, 快照保持不变; 首帧、拾取、选中环与相机均使用规划后的轨道位置.
- 新回归覆盖确定性、输入重排、周期一致性、Kepler 常量、重合布局种子与不同时间穿过同一区域.
  144 颗行星以 2053 个独立偏移时刻检查完整约 634.70 虚拟秒周期, 测试半径 0.82 时测得最小表面间距约 0.346.
  此采样是独立回归验证, 生产规划另外以曲线误差包络覆盖采样间隙; 不以截图代替间距证明.
- `pnpm check` 通过: 84 处导入边界、53 项测试、格式、严格类型和生产构建.
  浏览器总览、Atlas 聚焦、选择行星、暂停与推进正常, 控制台无警告或错误.
  初始化增加规划计算; 九批实例的逐帧矩阵上传与原有求解流程保持, 未进行新一轮硬件 GPU 性能资格测试.
- 没有安装依赖或改动 GLB, 既有 LAN 服务继续运行; 稠密输入在有界搜索无解时报告失败, 不静默放行冲突轨道.

## 2026-09-10 启用恒星基础自转

- 可用恒星的默认自转设为 `2π/60` 弧度/秒, 即一分钟一圈, 统一配置于 `runtime/config.ts`.
  各节点仍可通过既有接口覆盖或停转; 不随行星数量变化.
- 更新既有回归, 验证不同数量的恒星在 15 秒完成四分之一圈、60 秒完成整圈, 独立覆盖速度继续有效.
  完整 `pnpm check` 通过: 81 处导入边界、50 项测试、格式、严格类型和生产构建.
- 浏览器聚焦 Atlas 后确认无需设置速度即可自转, 选中行星保持公转暂停, 控制台无警告或错误.
  未安装依赖, 已有 LAN 服务继续运行.

## 2026-09-10 恒星数量缩放与自转接口

- 以 30 颗为当前模型尺寸, 按 10/30/60/120/180 颗映射 0.5/1/1.5/2/3 倍, 分段线性插值并限制两端.
  恒星表面、日冕与拾取同步缩放; 当前演示 Atlas/Lyra/Vega 分别为 1.1/1.3/1.5 倍.
- 预留按 Peer ID 设置弧度/秒的自转接口, 默认 0, 无数据驱动的速度规则. 支持停转、反转和无效输入拒绝.
  自转与公转时钟独立, 不改变行星位置、黑洞姿态、选择或相机; 页面隐藏仍由现有帧循环暂停.
- 新回归覆盖所有指定数量节点和中点、两端钳制、缩放后的实际射线拾取、日冕同步、快照不变,
  自转帧率独立、停转/反转、非法时间和速度、极大有限输入及共享源模型不变.
- 浏览器验收确认总览大小差异与 Atlas 聚焦; 调用接口后恒星表面转动, 选中行星保持轨道静止,
  停转接口返回成功. 控制台无警告或错误; 未进行本次硬件 GPU 性能资格测试.
- 完整 `pnpm check` 通过: 80 处导入边界、50 项测试、格式、严格类型检查和生产构建.
  未安装依赖或修改 GLB, 既有 LAN 服务继续运行.

## 2026-09-10 流纹三倍加速与暖白中心

- 实例展示时钟乘三, 同步加速纹理平流和亮结生命周期; 两相位流场的最大剪切量保持不变.
  回归确认一秒推进三个虚拟秒、分帧结果一致、相位包裹和实例时钟隔离. 行星仍为 5/2/停止.
- 远景 B 通道改为分段流团并混合过滤后的细丝能量, 去掉对低密度尾部的提亮, 减少连续光滑亮带.
  高温内缘和中心流改为更亮的暖白色; 按后续反馈为外层补回少量橙红, 未改模型尺寸.
- 固定页面查看距离 48 的 12 度近景及距离 192 的远景, 并播放 45 度远景流纹; 控制台无警告或错误.
  这些人工画面对照不构成连续运动抗闪烁或 GPU 性能资格测试. 黑洞仍为一次绘制, 没有新增纹理查询或帧循环.
- 最终源码的 `pnpm check` 通过: 80 处导入边界、47 项测试、格式、严格类型和生产构建.
  未安装依赖或下载资产, 已有 LAN 服务继续监听 `0.0.0.0:5173`.

## 2026-09-10 远景稀疏亮流束

- 远景从宽云团强度切换到 B 通道烘焙的稀疏流束, 保留更宽的明暗间隔; 近景 R 通道细丝与 G 通道温差继续使用.
  测试中的规则同心亮带过于接近霓虹圈, 已改为二维噪声形成的不规则流束, 不保留那一版规则光圈.
- 远景额外导数模糊从 2 倍减为 1.15 倍, 保留 mipmap、各向异性和接缝过滤; 发射系数从 1.9 调为 2.15.
  局部流束峰值单独提高, 不把整个星图统一曝光. GLB、纹理尺寸和绘制调用数不变, 复用原先空闲的 B 通道.
- 固定页面在参考 12 度检查距离 48、96、192, 并检查远景 45 度与播放流纹时的采样画面.
  远景亮部及暗带比原先更可辨, 中近景保留细纹; 控制台无警告或错误, 黑洞仍为一次绘制.
  本次背景验收窗口刷新偏低, 这些截图不作为连续动画无闪烁或 GPU 性能资格证明.
- `pnpm check` 通过: 80 处导入边界、47 项测试、格式、严格类型检查和生产构建.
  未新增依赖或下载资产, 未重新执行生产浏览器预览和完整交互矩阵. LAN 开发服务继续监听端口 5173.

## 2026-09-10 椭圆轨道、5/2/停止与黑洞层次

- 行星从球壳整体转动改为独立 Kepler 椭圆, 初始位置不跳变, 恒星位于焦点, 近星段快、远星段慢.
  最新倍率为总览 5、恒星视图 2、行星详情 0; 选中行星暂停所有公转, 返回后从当前相位继续.
- 拾取列表在总览只包含恒星, 星系视图仅加入所属行星. 控制器的选择和推进接口同样拒绝总览和跨星系请求.
  当前矩阵同时用于绘制、拾取、选中环和推进; 完整轨道包围球避免运动后的错误剔除.
- 黑洞中央阴影按用户选择加入弱弧面明暗, 并被盘像遮挡; 这是风格化表现, 不是物理表面发光.
  流纹与亮结的速度提高到前一版本的两倍, 远景保留宽云团和宽亮结, 极小节点再淡出运动亮结.
  本次未改动 GLB 或添加纹理, 黑洞仍为一次绘制.
- `pnpm check` 通过: 80 处导入边界、47 项测试、格式、严格类型检查和生产构建.
  新回归覆盖轨道焦点、平面、周期、速度与面积、初始连续性、5:2 时钟、暂停无上传、分层射线拾取和远景亮结推进.
- 完整开发页面加载与恒星点击推进正常, 1280×720 当前远程浏览器总览约 32 FPS, 控制台无警告或错误.
  生产控制器验收页面确认总览选择/推进被拒绝、跨星系被拒绝、所属行星选择成功、暂停后画布点击切换行星成功.
  移动中小行星的人工点击受工具延迟影响, 未将点击未命中的尝试记为成功; 运动后射线拾取由真实 Three.js 回归验证.
- 独立椭圆公转使实例矩阵改为每批每帧上传一次, 当前 144 实例约 9 KiB/帧; 暂停时不计算行星轨道或上传矩阵.
  没有安装依赖或下载资产, LAN 服务保留在 `0.0.0.0:5173`.
  本次未重新进行生产浏览器预览、完整故障矩阵或大规模 GPU 性能资格测试.

## 2026-09-10 校正发射盘、频移与流纹, 缩小星图黑洞

- 盘内缘调整到非旋转模型的 `3 rs`, 外盘延伸到 24 并从 19.5 渐暗, 最大半厚度为 0.28.
  重新生成 GLB, 恒星和行星资产保持不变; 星图黑洞整体缩小 25%, 拾取代理同步缩放.
- 亮度和温差依据弯曲光路的守恒角动量计算; 两次交点共用同一发射函数和遮挡规则.
  流纹改为二维周期密度场, 极窄次像的代表半径和能量在初始化时积分, 局部相机位置在实例绘制前绑定.
- 固定页面检查参考、正负侧视、俯视及中远景. 测试中的弯曲体积积分产生条带且显著降低帧率, 已撤回;
  最终仍使用薄盘交点和四步局部前景盘缘, 不声称解决完整有限厚度辐射转移.
- `pnpm check` 通过: 78 处导入边界、42 项测试、格式、严格类型检查和生产构建.
  新增覆盖实际 GLB 盘尺寸、窄像能量范围和多实例局部相机变换的断言.
  完整开发星图加载正常; 缩小后的黑洞点击聚焦与旋转正常, 不自动打开详情.
  浏览器控制台无警告或错误, 当前远程浏览器总览及正常聚焦约 32 FPS.
  此记录不作为硬件 GPU 上的 60 FPS 或完整交互、故障、容量矩阵资格证明.
- 未新增依赖或下载第三方资产. 局域网开发服务监听 `0.0.0.0:5173`.

## 2026-09-10 修正中远景宽光弧的实心带与硬边

- 复现倾角 -2 度、距离 96 时上半光弧变成灰色实心带的问题. 原先按节点整体细节级别替换次像,
  将仍可分辨的宽光弧按固定发热半径填满, 丢失了盘缘和径向发射梯度; 上一轮验收漏掉了负倾角的中远景.
- 次像始终查询真实盘面交点, 仅在该处像宽接近像素足迹时混入覆盖近似. 混合颜色按覆盖率加权,
  避免透明盘缘混入固定白热颜色. 节点距离只控制亮结和纹理细节, 不再替换宽光弧的形状.
- 在 1280×720 隔离页面固定流纹时间, 对照倾角 -2 度下距离 48、96、192、384, 以及距离 96 下的 +2、0、+12 度.
  中远景没有再出现原先的实心上弧; 控制台无警告或错误, 仍为一次绘制. 临时对照页面已删除.
  两端仍随倾角投影收窄, 本次没有把它改成等宽光环, 也不声称实现完整有限厚度的相对论盘像.
- `pnpm check` 通过: 77 处导入边界、42 项测试、格式、严格类型检查和生产构建.
  本次没有新增依赖、下载或修改 GLB, 没有重新运行生产浏览器预览、完整交互矩阵或性能资格测试.
  取消了远景跳过次像交点查询的优化, 优先保留轮廓; LAN 开发服务继续在端口 5173 提供当前源码.

## 2026-09-10 有限盘缘、差速流纹与距离细节

- 前景盘缘增加有限厚度, 最大半厚度 0.16, 与重新生成的 GLB 截面一致; 黑洞资产为 1,217,688 字节.
  恒星和行星 SHA-256 保持不变. 四步局部体积采样按光程间距选择 mip, 修正正侧视横向条码闪烁.
- 模型姿态固定, 流纹改为内快外慢的双相位差速流场, 并加入三个生成、拉长和消散的亮结.
  初始化纹理增加角向密度变化, 降低整圈均匀发白的观感. 绘制前绑定实例时钟, 不克隆共享材质和纹理.
- 阴影投影半径控制次像、细纹和亮结细节; 精确侧视附近消除倾角舍入噪声, 修正近景交点切换形成的碎斑.
  隔离浏览器页面检查 +12、+0.5、0、-0.5、+45 度和距离 18 的正侧视; 距离 320 确认 detail=0, 仍为一次绘制.
  最终控制台无警告或错误, 临时验收页面已删除.
- `pnpm check` 通过: 77 处导入边界、42 项测试、格式、严格类型检查和生产构建.
  新回归覆盖多实例共享材质的独立时钟、强制 uniform 刷新、帧率独立性、无效增量和距离细节.
  生产预览检查完整星图加载、Orion 无所属行星和连线、点击聚焦不打开详情及旋转; 控制台无警告或错误.
- 当前远程浏览器生产总览和正常聚焦约 32 FPS, 隔离大幅近景约 15-18 FPS, 尚未证明硬件 GPU 上的 60 FPS.
  有限厚度只补充近侧视前景, 弯曲盘像仍为薄盘近似; 本次未重新执行完整浏览器故障矩阵、行星详情或容量压测.
  没有新增依赖或第三方资产下载, 临时生产预览已停止, LAN 开发服务继续保留在端口 5173.

## 2026-09-10 流纹加速与穿过盘面时的双侧可见性

- 流纹角速度从 0.025 提高到 0.075 弧度/秒, 从约 251 秒一圈变为约 84 秒一圈; 不增加绘制调用、采样或纹理上传.
- 修正近乎侧视时第二交点被统一压暗的问题. 接近盘面时连续恢复其可见度, 两次交点换序时上下盘像能够同时显现.
- 隔离页面检查倾角 +2、+0.5、0、-0.5、-2 和 +12 度, 浏览器控制台无警告或错误; 临时页面验收后删除.
  当前仍是薄盘近似, 精确零倾角时前景盘缘会退化, 有限厚度尚未实现. 不将本次修改解释为物理光速或事件视界表面动画.
- `pnpm check` 通过: 74 处导入边界、41 项测试、格式、严格类型检查和生产构建.
  本次没有重新运行生产预览、完整浏览器故障矩阵或硬件 GPU 压测; 未新增依赖, LAN 服务仍在端口 5173.

## 2026-09-10 修正黑洞半球与重复盘面成像

- 根据正面、背面和俯视截图, 移除普通核心球面、实体盘和独立光弧的重复表面绘制.
  GLB 的封闭光学体积统一计算前两次盘面交点及捕获阴影, Core 保留拾取, Disc 与 Glow 保留离线建模契约.
  重建光学体积和盘内缘, 黑洞 GLB 为 1,217,692 字节; 恒星、行星 SHA-256 保持不变.
- 光轨道表增加临界值以下的捕获路径, 捕获后不再返回零逆半径; 新增有限距离相位表.
  次像使用连续过滤和集中于临界阴影附近的亮度分布, 降低宽亮下环; 周期流纹接缝修正纹理采样导数.
  这些是实时成像和美术调整, 不代表完整物理模拟或环境星体折射.
- 最终 `pnpm check` 通过: 74 处导入边界、41 项测试、格式、严格类型检查和生产构建.
  回归覆盖捕获终止、临界采样连续性、有限距离相位、隐藏核心拾取和共享纹理释放.
- 隔离页面检查参考、45 度俯视、背面、顶部和相机距离 8 的近景, 不再出现凸起的半球表面; 临时页面已删除.
  生产预览检查完整星图、Orion 独立状态、点击聚焦不打开详情, 并复核旋转与双击返回.
  最终页面控制台无警告或错误; 当前远程浏览器约 32 FPS.
- 场景遮挡使用节点附近的虚拟深度, 不计算外部星体与弯曲光线的交点; 未重新执行完整浏览器故障矩阵、运动中行星详情或硬件容量压测.
  没有新增依赖或下载第三方资产, LAN 开发服务继续保留在端口 5173.

## 2026-09-10 精细黑洞模型与光轨道盘像验收

- 重制核心与连续吸积盘网格, 黑洞 GLB 为 1,217,704 字节; 恒星和行星 GLB 的 SHA-256 保持不变.
  弯曲盘像查询初始化时积分的光轨道, 与实体盘共享带 mipmap 的流纹; 高阶细像按像素覆盖面积过滤.
  调整俯视时的混合权重, 避免背面盘像和实体盘叠出第二张盘. 这是经艺术调整的混合渲染, 不是完整物理模拟.
- `pnpm check` 通过: 73 处导入边界、39 项测试、格式、严格类型检查和生产构建.
  新增平直空间解析对照、捕获与逃逸、临界细像范围检查; 资源测试确认查找表和流纹共享且只释放一次,
  盘面与光学球壳旋转后仍使用一致的纹理坐标.
- 临时隔离页检查参考视角、斜向和俯视旋转、连续近距离放大; 已关闭并删除该页面.
  生产预览检查四节点总览、Orion 无所属行星和连线、侧栏折叠、黑洞聚焦不打开详情、旋转和双击返回.
  浏览器控制台无警告或错误. 当前远程浏览器显示约 32 FPS, 不作为本机硬件 GPU 或大规模场景的性能资格证明.
- 本次没有重新执行完整浏览器故障矩阵、运动中行星详情流程或大规模 GPU 压测.
  没有安装依赖、下载模型或加入全屏后处理; 开发服务继续监听 `0.0.0.0:5173`.

## 2026-09-10 黑洞连续盘面与局部辉光调整

- 按视觉反馈去掉独立螺旋管线, GLB 改为 Core、Horizon、Accretion 下的 Disc 与 Glow.
  盘面使用连续云纹和橙白温差, 光弧加入柔和边缘, 椭球内固定六次采样局部辉光并按核心截断光程.
- 黑洞资产由 578,952 字节降至 403,752 字节; 恒星与行星文件的 SHA-256 未改变. 资产更小不代表 GPU 工作量同比下降.
- `pnpm check` 通过: 67 处导入边界、36 项测试、严格类型检查和生产构建.
  浏览器检查总览、黑洞聚焦、近景缩放、斜向旋转和双击返回, 控制台无警告或错误.
  本次没有重新执行生产预览、完整浏览器故障矩阵或大规模硬件 GPU 压测.

## 2026-09-10 黑洞吸积盘与弯光外壳验收

- 重制黑洞 GLB 的薄体积吸积盘、渐细流束和球形光学外壳. 恒星与行星模型文件未改变.
- 上方主光弧、下方细环、盘面两侧亮度随观察角度变化; 黑色核心保持不透明, Orion 仍无行星或连线.
  这是轻量解析视觉近似, 不代表完整广义相对论模拟.
- `pnpm check` 通过: 67 处导入边界、36 项测试、严格类型检查和生产构建.
  真实 GLB 回归补充旋转盘面与光学轴对齐检查; 浏览器检查聚焦、近景滚轮缩放、斜向旋转和双击返回,
  控制台无警告或错误. 本次未重新执行完整浏览器故障矩阵、运动中行星详情验收或大规模 GPU 压测.

## 2026-09-10 星体模型与不可用节点验收

- Atlas、Lyra、Vega 维持可用, Orion 为不可用黑洞; 共四个 Peer、144 个实体和三条邻接边.
- 恒星、行星、黑洞均加载真实 GLB 网格, 不使用星体或日冕 Sprite. 所有资产包含内置缓冲, 无外部纹理.
- Orion 没有行星或连线; 即使输入快照保留旧实体和边, 也不创建其行星网格、拾取入口、详情或连线.
- 模型核心可聚焦并遮挡后方对象, 旋转时可观察吸积盘厚度和前后关系; 双击仍返回总览, 不显示 X 标记.
- 模型加载在画布创建前完成, 加载前取消和加载中取消均不创建画布; 加载失败走既有错误与重试入口.
- `pnpm check` 通过: 67 处导入边界、36 项测试、严格类型检查和生产构建. 浏览器确认恒星与黑洞聚焦、黑洞旋转和双击返回;
  生产预览成功加载三类模型, 无警告或错误. 本次未重新完成运动中行星的浏览器详情验收, 实际 GLB 世界坐标拾取由回归测试覆盖.
  加载取消和 HTTP 错误由自动测试覆盖, 尚未重新执行完整浏览器故障矩阵或大规模 GPU 压测.

## 拖动平滑性回归

- 同一段拖动每帧拆为 1 次或 16 次输入, 相机逐帧轨迹一致, 输入事件本身不修改相机.
- 旋转和平移在 30/60/120 FPS 下按相同时间收敛, 不超调或来回振荡.
- 聚焦前清空旧惯性不改变当前姿态, 推进结束后不被旧旋转或平移拉回.
- 聚焦中按下鼠标即停止推进, 第一像素拖动不会继续与动画竞争; 点击容差仍然保留.
- `pnpm check` 通过: 57 处导入边界、32 项测试、类型检查和生产构建.
  浏览器验证推进中拖动接管和近景旋转, 恒星保持在旋转中心, 控制台无警告或错误.

## 2026-09-09 基础骨架验收记录

- Node 24.21.0 / pnpm 12.3.4: `pnpm check` 通过, 包括 54 处导入边界、28 项回归测试、类型检查和生产构建.
- 已有浏览器验证恒星推进、滚轮缩放、详情类型切换、列表单选、行星推进、双击返回和侧栏折叠.
  为避免截图与点击间的工具延迟导致错过运动中的小行星, 详情流程曾临时冻结公转;
  随后已恢复正常公转并完成上述完整检查. 公转后的拾取和暂停另有真实 Three.js 数学测试覆盖.
- 临时生命周期验收页验证真实 `WEBGL_lose_context` 丢失与重试、卸载/重挂载、空快照、非法快照和有效快照恢复.
  卸载和非法快照后画布数为零, 重建后为一; 临时页面已删除.
- 本地 `pnpm preview` 等效入口验证生产星图和动态渲染器分包正常加载, 控制台无警告或错误,
  开发用帧计数属性未进入生产画布.
- 实际浏览器后台暂停、冷启动异步 import 与卸载竞争、初始化中途资源申请失败尚未逐项故障注入.
  现有假时钟和资源作用域测试覆盖相应基础机制, 不声称完整浏览器故障矩阵已通过.

## 性能记录

记录 CPU、GPU renderer、浏览器版本、显示刷新率、画布尺寸和 DPR, 分别测空场景 RAF 与完整场景.
目标 60 FPS 不意味着软件渲染或约 32 Hz 的远程显示也能输出 60 FPS.
本次基础骨架整理不增加模型规模, 不把小规模示例视为生产性能资格测试.
