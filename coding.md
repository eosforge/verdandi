# 通用开发规范

授权与项目环境遵循 [AGENTS.md](AGENTS.md). C++ 命名、作用域、布局、注释和排版只在 [cpp-coding.md](cpp-coding.md) 定义, 本文不重复或覆盖. 组件入口见 [文档导航](docs/README.md).

## 适用范围与组织

- 活动实现为 C++ Star/Pulsar/Comet、Go Polaris/Astrolabe, 管理前端为 TypeScript/Vue, Python 提供构建与测试工具. 具体完成度以 docs/progress.md 为准, 不把已写源码当作已验证交付. Moon/Planet、Comet Go 和完整 Orrery 暂缓; 旧 Rust 服务与 Redis SDK 冻结, 已删除的 Go Supervisor 只从 Git 历史查阅.
- 按职责和所有权组织模块, 默认私有. 真实的多个使用者出现后才抽取共用层, 不为目录形式建立空接口、包装或管理类.
- 各语言使用原生错误、取消、并发和资源机制; 统一协议行为与生命周期, 不强制相同的内部代码形状.
- 优先用已支持的现代语言和标准库能力减少工作、分配和复制. 工具链下限变化单独评审, 不借此自动升级依赖.
- 生成源码只通过规范输入和生成器更新; 第三方代码与许可证不手改. 业务头文件不暴露私有网络或密码库类型.

## 正确性与资源

- 外部无效输入、网络失败、容量不足及取消返回稳定错误, 不以 panic、契约终止或未捕获异常处理可恢复失败.
- 转换、计数、长度、偏移和分配运算检查边界. 队列、重试、解码和临时结果有明确资源预算.
- 每个任务、线程、RPC、定时器、缓冲区和连接都有所有者. 取消后仍须等待完成, 再销毁被借用对象; close/cleanup 保持幂等.
- 明确锁顺序与回调线程. 不在普通状态锁下执行外部回调、网络/文件 I/O 或等待线程退出; 例外必须有实际不变量和有界性依据.
- 不确定的写入结果不得当作明确失败后盲目重放. 重试需要幂等或恢复规则, 并采用有界退避与抖动.
- 不可变视图不能通过隐含别名修改; 借用缓冲、回调和外部 API 明确寿命、重入、线程与异常边界.
- 性能复杂度必须有热点依据, 区分推断和测量. 不声称完整零拷贝、无锁、实时性或容量保证而不注明范围.

## 注释与格式化

- 手写生产注释当前使用中文与 ASCII 标点, 文件头说明当前职责. 不自动进行发布前英文转换. C++ 的测试与模板同样遵循其独立规范.
- 参数与配置按实际情况说明单位、范围、默认或初始值、零/空的含义. 每个枚举元素有对应说明, 不因整理改变值或顺序.
- 注释说明契约、原因、状态变化、锁边界和资源交接, 不写入尚未实现的保证. 公共声明与实现各说明自己的内容.
- 修改手写源码后立即运行已有的所属格式化器. C++ 使用对应目录 clang-format; Go 使用 gofmt; Rust 使用 rustfmt; Python 使用 Black; Admin 使用 Prettier.
- 非 C++ 的列宽和语法遵循所属已提交配置, 通常上限 160; C++ 允许超长行, 不套用通用列宽限制.
- Python 修改后立即 Black 格式化, 再做静态语法核对. 行为测试及其前置构建仍需本轮授权. 不引入仓库外缓存或修改 shell 全局环境.

## 语言边界

| 语言/组件 | 重点约束 |
| --- | --- |
| C++ | 见独立规范及 [Astra 维护指南](astra/CONTRIBUTING.md); RAII, expected, 生命周期与异常提交边界 |
| Go 服务与 Comet Go | gofmt, 明确 Context 与 goroutine 退出, 不在短锁内做 KDF/慢 I/O; 不暴露共享可变视图, 修改协议同时检查 C++ 消费端 |
| Rust 生成器 | rustfmt/Clippy; 不对外部输入 unwrap/expect, unsafe 必须解释调用及寿命前提 |
| Python | 子进程、SSH、临时资源归属明确; 保留 shell/协议字节, 不吞掉失败、跳过或清理错误 |
| Admin | strict TypeScript, Vue 与 Three.js 生命周期分离; 每个监听器、RAF 与 GPU 资源显式释放, 见组件贡献指南 |
| Proto | 字段说明单位、合法范围、所有者与版本含义; 不复用 reserved 字段, 生成文件不手写 |

旧 SDK 的语言版本、Lua 与 C ABI 规则仅适用于冻结实现, 不自动成为 Comet 契约. 不将旧 C++23/160 列限制重新施加到 Astra C++26.

## 审查、验证与证据

- 每次修复检查所有相关语言、生产者、消费者、共享核心、绑定及测试工具中的同类路径. 记录受影响、不适用、未发现对应问题或无法验证, 不因语言不同省略扩散审查.
- 修复应有能重现边界的行为用例. 协议用共享向量, 并发覆盖取消、重连、迟到完成和清理. 不以数量或镜像实现的断言冒充完整覆盖.
- 具体命令见 [Testkit](testkit/README.md), 运行前按 AGENTS.md 获得当轮授权. 未下载的工具或依赖不得隐式安装.
- 报告实际源码/二进制身份、环境、执行范围、失败和清理; 未运行、条件跳过及未实现功能不能写成通过. 回归不等于覆盖率或性能测量.
- 发现问题后重新检查改动是否引入新的缺陷; 修改协议同时核对所有活动消费者及生成一致性. 冻结组件只在实际受影响时扩展审查.
- 规范、设计和最新验证更新到所属稳定文档; 旧过程使用 Git 历史, 不累计新的日期化 MD. 不在没有用户指令时 commit 或 push.

## 配置、依赖与秘密

工具和缓存使用所属项目脚本传给子进程, 不写入用户或系统配置. 普通构建/检查只使用已有依赖, 下载与安装必须具体授权.
不提交真实账号密码、签名私钥、环境文件、数据库或个人 IDE 状态. 公开测试身份只用于隔离夹具; 日志和诊断不输出秘密或任意远端错误正文.
依赖调整明确版本、来源、许可证、用途和缓存位置, 不顺带升级其他包. C++ TLS/加密使用 gRPC 配套 BoringSSL, 不另混用独立 OpenSSL 运行库.
